<%= render partial: 'side', locals: { user: current_user, book: @book } %>


<h1>Users</h1>

<% @users.each do |user| %>


<p>image</p>
<%= image_tag user.get_profile_image(100,100) %>

<p>name</p>
<span><%= user.name %></span>

<%= link_to "Show", user_path(user.id) %></p>


<% end %>